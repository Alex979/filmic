{
  "name": "filmic-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "bun --filter horizon dev",
    "build": "bun --filter horizon build",
    "typecheck": "bun --filter filmic typecheck",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxlint": "^1.81.0",
    "typescript": "~6.0.2"
  }
}
